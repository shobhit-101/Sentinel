// Shared form-control styles, so inputs/labels stay consistent.
export const inputClass =
  'w-full bg-background border border-border rounded-lg px-3 py-2 text-sm ' +
  'placeholder:text-textFaint focus:outline-none focus:border-accent ' +
  'focus:ring-1 focus:ring-accent/40 transition-colors';

export const labelClass = 'block text-sm font-medium text-textMain mb-1.5';

export const hintClass = 'text-xs text-textMuted mt-1';
